remix mode prompt:
"All right." Next, what I'm going to do with Nano Banana is I'm going to upload this as a reference ad because I want Nano Banana to create a very similar style ad, but I want it to use obviously our product image, which I will attach, and I want the messaging to target one of our avatars. So, can you give me a prompt for that? Again, I'm going to upload two images already to Nano Banana Pro. Number one is this reference ad image, and then number two is the image of the Drio heater.

entonces con esto quedaria temrinada la comparaacion con los 25 prompts para genrar la app? si terminamos por favor actualiza el todo para reflejar lo implementado

---

1. Project foundation
Build me a Node.js web app using Express. It needs a clean, dark-themed UI (dark slate background, white text) served from a single HTML file. Set it up to run on Replit with PostgreSQL. The app is called "Meta Ads Generator."
2. Multi-brand client system
Add a client/brand switcher to the top of the app. Users should be able to create multiple brands, switch between them, and each brand's data should be completely separate. Store this in the database.
3. Brand kit setup
Add a Brand Setup section where users can enter their brand name, description, primary and secondary colors, font preferences, and a short description of their target audience. Save this to the database and use it throughout the app.
4. Logo and asset uploads
Add file upload functionality so users can upload their brand logo and product images. Store the files on the server and show thumbnails in the UI. Support JPG, PNG, and WebP.
5. FAL AI image generation
Integrate the FAL AI API (fal-ai/nano-banana-2/edit) to generate images. It takes multiple input images and a text prompt and returns a generated image. Add my FAL_KEY as an environment secret.
6. Basic ad generator — Remix mode
Build the core generation flow: user uploads a "remix reference" image (an existing ad they want to remix) and a product image, writes optional instructions, picks an aspect ratio (square, portrait, landscape), and hits Generate. Send both images + a prompt to FAL AI and display the result.
7. Bulk generation
Update the generator so users can request 1 to 60 ads in a single run. Generate them all sequentially and display each one as it completes, like a live feed. Show a progress counter.
8. Generation history board
Add a history board that shows all previously generated ads for the current brand as a masonry/grid layout. Load it on startup. Each image should be clickable to view full size.
9. Download button
Add a download button to every generated ad. When clicked, it downloads the image file directly to the user's computer with a clean filename.
10. Google Gemini prompt composer
Integrate the Google Gemini API. Before sending a prompt to FAL AI, use Gemini to write a better, more detailed creative direction prompt based on the user's brand kit, reference image, and instructions. Add GEMINI_API_KEY as a secret.
11. Brand intelligence profiles
Add a Brand Intelligence section where users can define customer personas — each with a name, pain points, emotional triggers, and marketing angles. Store these in the database. Let users add, edit, and delete profiles.
12. Reference ads library
Add a "Reference Ads" section in Brand Setup. Users can upload their best existing ads as persistent references. During generation, these are automatically described to the AI as examples of on-brand advertising.
13. Ad format library
Create a library of 16 named ad formats (UGC, testimonial, bold billboard, before/after, etc.) each with example images, visual rules, and copy guidelines. Store these in a JSON file. Show them as selectable cards with thumbnails.
14. Concept Library
Build a Concept Library where users can see all 16 built-in ad formats plus add their own custom concepts by uploading example images. Use Gemini to automatically analyze uploaded examples and generate metadata (description, visual structure, copy guidance, rules).
15. Concepts mode — AI planner
Add a second generation mode called "Concepts Mode." Instead of a single reference image, the AI planner (Gemini) creates a concept plan: it picks different ad formats from the library, pairs each with a customer persona, and writes a hook and angle for each. Show the plan to the user before generating.
16. Concept plan preview UI
Build a plan review screen that shows each planned concept as a card with the format thumbnail, persona, angle, hook preview, and how many ads will be generated for it. Let users remove concepts they don't want before approving.
17. Concept mode generation
Execute the approved concept plan — generate all the ads, one per concept variation, using the right format example images as references. Show them live as they complete. Each ad should know which concept and persona it came from.
18. Ad detail modal
When a user clicks any ad, open a full-screen modal with two panels: the large image on the left, and structured metadata on the right — brand, mode, format, angle, persona, hook variants, body copy, CTA, strategic rationale, and the AI prompt used.
19. "Remix This" action
Add a "Remix This" button inside the ad detail modal. When clicked, it closes the modal, switches to Remix mode, and loads that ad as the new remix reference automatically — so users can instantly iterate on any generated ad.
20. Save as Template
Add a "Save as Template" button to ads. Saved templates appear in a Templates section and can be reused as remix references for future campaigns.
21. Brand reference auto-injection
Update the generation prompt logic so that brand reference ads are automatically fetched and described to Gemini as "this is what on-brand advertising looks like for this brand" — a soft style influence that runs in the background without the user needing to do anything.
22. Delete and clean up
Add delete functionality for generated ads, templates, assets, and brand intelligence profiles. Deleting an ad should remove both the database record and the image file from the server.
23. Layout guard and style blueprint
When a reference image is provided, use Gemini vision to analyze its layout — is it a single hero image, split screen, before/after? Extract a style blueprint. Then enforce that layout in the generation prompt so the AI can't accidentally change the panel structure.
24. Human archetype protection
If a concept example image contains a person (doctor, customer, influencer), the AI tends to copy their face. Fix this by having Gemini extract the "archetype role" (e.g. "medical professional in white coat") and instruct the image model to cast a completely different person who fills the same role — different face, age, ethnicity, pose.
25. Export for sharing
Build a shell script that packages the entire app as a ZIP file ready to import into any Replit account — with all concept library data exported, brand data wiped, no API keys included, and Replit config files pre-set so it runs immediately after import with no setup beyond adding secrets.

